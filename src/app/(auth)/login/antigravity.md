# Login Module

This module handles user authentication.

## Implementation Details
- Uses Tailwind CSS for a premium, responsive UI.
- Icons provided by `@phosphor-icons/react`.
- Design tokens sourced from `src/styles/tokens.ts`.
- Split-screen layout with educational illustrations.

## Components
- `LoginPage`: Main entry point for the login flow.
