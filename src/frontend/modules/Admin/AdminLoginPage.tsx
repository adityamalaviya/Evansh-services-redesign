// This code was created by a human and debugged by AI
"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { loginUser } from "@/lib/api/auth";import { useAuthStore } from "@/lib/store/authStore";
export default function AdminLoginPage(){const router=useRouter();const {setAuth,clearAuth}=useAuthStore();const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await loginUser({email:String(f.get("email")),password:String(f.get("password"))});if(r.user.role!=="admin"){clearAuth();setError("Access denied.");return;}setAuth(r.user,r.accessToken);router.push("/admin/dashboard");}catch{setError("Invalid email or password.");}}return <form onSubmit={submit}><input name="email" type="email" required/><input name="password" type="password" required/><button>Admin login</button><p>{error}</p></form>;}
